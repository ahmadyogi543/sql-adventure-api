export type UserRow = {
  id: number;
  username: string;
  password_hash: string;
  role: string;
};

export type User = {
  id: number;
  username: string;
  passwordHash: string;
  role: string;
};
