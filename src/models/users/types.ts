export type UserRow = {
  id: number;
  name: string;
  email: string;
  institution: string;
  password_hash: string;
  role: string;
};

export type User = {
  id: number;
  name: string;
  email: string;
  institution: string;
  passwordHash: string;
  role: string;
};
